# ===========================================
# AMBIENTE: PRODUÇÃO (nuvem)
# ===========================================
# Conecta no PostgreSQL do Supabase (projeto de produção).
# As credenciais serão configuradas como variáveis de ambiente no Render.

spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      # "validate" apenas confere se o schema do banco bate com as @Entity;
      # quem cria/altera tabelas é o Flyway (ver application.yml).
      ddl-auto: validate
    show-sql: false

logging:
  level:
    com.renlip.fiis: INFO
